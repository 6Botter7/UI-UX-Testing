import "../template/designer-repo/text-list.js";
import "../maze-escape/maze-sizes.js";

export default class designerRepo  extends crsbinding.classes.ViewBase {
    async connectedCallback() {
        await super.connectedCallback();
    }
}